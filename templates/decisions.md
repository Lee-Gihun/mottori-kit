# 결정 기록 (DR)

**이 워크스페이스의 결정만 담는다.** 킷 자체의 설계 결정은 `system/kit-decisions.md`에 있고
그건 업스트림 소유라 `git pull`이 덮는다. 이 파일은 git 밖이라 안 덮인다.

append-only. 형식: `### DR-NNN 제목 (YYYY-MM-DD · active|superseded_by:DR-MMM)` →
결정 / 맥락 / 기각 대안 / 참조. 건당 짧게. journal의 decision 줄이 `→ dr:NNN`으로 가리킨다.
스키마 정본: `../tools/memlib.py` · PRD: `PRD-session-memory.md`.

*(아직 비어 있다. 첫 결정이 나면 여기 쌓는다.)*
