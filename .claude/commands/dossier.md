복귀 의식: "$ARGUMENTS" 스레드의 서류철을 읽어줘.

등록된 스레드 목록은 `python3 tools/now.py threads`로 확인한다 (정본은
system/memory-config.json의 threads — 목록을 이 파일에 박아두지 않는다).

읽고 나서 ①현재 위치 ②확정 결론 ③미결 질문을 요약하고, 필요하면 recall로 원문을 보강한 뒤
논의를 시작해. 세션을 떠날 때는 마감 의식(델타 5줄 append)을 잊지 말 것.
