# Recall

Run `python3 tools/recall.py find "$ARGUMENTS"` to recover the original past conversation. Do not paste the
whole result. Translate only a few lines connected to the current discussion (PRD-session-memory section 3.4).
If there is no match, log a known-item failure in the journal for the embedding-gate count.
