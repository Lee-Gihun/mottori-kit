# NOW

Read `state/NOW.md` together with `_private/state/NOW.md` when the latter exists, then summarize the current
state. If the local overlay is missing, call it "unavailable", not "absent". Next run
`python3 tools/now.py check` and propose a repair for each warning. If the merged view conflicts with the
conversation context, the merged view is authoritative.
